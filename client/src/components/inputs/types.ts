export interface inputsDataState {
  category: string;
  date: string;
  description: string;
  amount: number;
  bank: string;
}

export interface inputsDataError {
  category: boolean;
  date: boolean;
  bank: boolean;
}
